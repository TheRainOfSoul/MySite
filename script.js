// Translations Dictionary
const translations = {
    ru: {
        page_title: "Универсальный IT-Специалист | Портфолио",
        logo_span: "folio",
        nav_about: "Обо мне",
        nav_skills: "Навыки",
        nav_projects: "Проекты",
        nav_contact: "Контакты",
        hero_greeting: 'Привет, я создаю <br><span class="text-gradient">цифровую магию</span>',
        hero_prefix: "Я ",
        hero_desc: "Объединяю код, искусственный интеллект и дизайн для создания крутых продуктов и решения сложных задач.",
        hero_btn_projects: "Мои проекты",
        hero_btn_contact: "Связаться",
        about_title: 'Обо <span class="text-gradient">мне</span>',
        about_subtitle: "Универсальный солдат IT",
        about_p1: "Я не ограничиваюсь одной сферой. Мой подход — это решение задач любыми эффективными способами. Я занимаюсь:",
        about_li1: "Разработкой современных веб и мобильных приложений",
        about_li2: "Профессиональным видеомонтажом",
        about_li3: "Внедрением нейросетей и ИИ в бизнес-процессы",
        about_li4: "Сборкой проектов на No-Code платформах",
        about_li5: "Ремонтом и настройкой ПК на железном уровне",
        about_p2: "Такой широкий спектр навыков позволяет мне видеть проекты целиком, от идеи и железа до финального интерфейса.",
        about_cv: "Скачать CV",
        skills_title: 'Мои <span class="text-gradient">Навыки</span>',
        skill_video: "Видеомонтаж",
        skill_hardware: "Ремонт ПК",
        projects_title: 'Мои <span class="text-gradient">Проекты</span>',
        project_img: "Изображение проекта",
        btn_view_project: "Подробнее",
        proj1_title: "AI Telegram Bot",
        proj1_short: "Многофункциональный бот с интеграцией нейросетей для генерации контента.",
        proj2_title: "No-Code Marketplace",
        proj2_short: "Платформа электронной коммерции, собранная полностью на зеро-код инструментах за 2 недели.",
        proj3_title: "Коммерческий видеомонтаж",
        proj3_short: "Серия динамичных рекламных роликов с моушн-дизайном.",
        contact_title: 'Останемся <span class="text-gradient">на связи</span>',
        form_name: "Имя",
        form_msg: "Сообщение",
        form_btn: "Отправить сообщение",
        social_title: "Соцсети и ссылки",
        social_desc: "Готов к новым проектам и сотрудничеству. Отвечаю быстро!",
        footer_text: "&copy; 2026. Создано с любовью и технологиями.",
        btn_github: "Смотреть на GitHub",
        btn_live: "Live Demo",
        dynamic_texts: [
            "разработчик приложений.",
            "специалист по ИИ.",
            "создатель No-Code систем.",
            "мастер видеомонтажа.",
            "эксперт по железу."
        ],
        proj_updated: "Обновлён",
        proj_private: "Приватный",
        proj_project_word: "проект",
        proj_no_desc: "Описание пока не добавлено.",
        proj_error: "Не удалось загрузить проекты.",
        proj_view_profile: "Открыть профиль на GitHub"
    },
    en: {
        page_title: "Versatile IT Specialist | Portfolio",
        logo_span: "folio",
        nav_about: "About",
        nav_skills: "Skills",
        nav_projects: "Projects",
        nav_contact: "Contact",
        hero_greeting: 'Hi, I create <br><span class="text-gradient">digital magic</span>',
        hero_prefix: "I am ",
        hero_desc: "Combining code, artificial intelligence, and design to build great products and solve complex problems.",
        hero_btn_projects: "My Projects",
        hero_btn_contact: "Get in Touch",
        about_title: '<span class="text-gradient">About</span> Me',
        about_subtitle: "Versatile IT Soldier",
        about_p1: "I'm not limited to one field. My approach is solving problems through any effective means. I specialize in:",
        about_li1: "Modern Web and Mobile App Development",
        about_li2: "Professional Video Editing",
        about_li3: "Integrating Neural Networks and AI into Business",
        about_li4: "Building on No-Code Platforms",
        about_li5: "Hardware Level PC Repair and Setup",
        about_p2: "Such a broad spectrum of skills allows me to see projects as a whole, from the hardware to the final interface.",
        about_cv: "Download CV",
        skills_title: 'My <span class="text-gradient">Skills</span>',
        skill_video: "Video Editing",
        skill_hardware: "Hardware Repair",
        projects_title: 'My <span class="text-gradient">Projects</span>',
        project_img: "Project Image",
        btn_view_project: "View Details",
        proj1_title: "AI Telegram Bot",
        proj1_short: "Multifunctional bot with AI integration for content generation.",
        proj2_title: "No-Code Marketplace",
        proj2_short: "E-commerce platform built entirely on no-code tools in 2 weeks.",
        proj3_title: "Commercial Video Montage",
        proj3_short: "A series of dynamic promo videos with motion design.",
        contact_title: 'Let\'s Stay <span class="text-gradient">Connected</span>',
        form_name: "Name",
        form_msg: "Message",
        form_btn: "Send Message",
        social_title: "Socials & Links",
        social_desc: "Ready for new projects and collaboration. I reply fast!",
        footer_text: "&copy; 2026. Built with love and tech.",
        btn_github: "View on GitHub",
        btn_live: "Live Demo",
        dynamic_texts: [
            "an app developer.",
            "an AI specialist.",
            "a No-Code builder.",
            "a video editing master.",
            "a hardware expert."
        ],
        proj_updated: "Updated",
        proj_private: "Private",
        proj_project_word: "project",
        proj_no_desc: "No description yet.",
        proj_error: "Could not load projects.",
        proj_view_profile: "Open GitHub profile"
    },
    hy: {
        page_title: "Բազմակողմանի ՏՏ մասնագետ | Պորտֆոլիո",
        logo_span: "folio",
        nav_about: "Իմ մասին",
        nav_skills: "Հմտություններ",
        nav_projects: "Նախագծեր",
        nav_contact: "Կապ",
        hero_greeting: 'Ողջույն, ես ստեղծում եմ <br><span class="text-gradient">թվային մոգություն</span>',
        hero_prefix: "Ես ",
        hero_desc: "Միավորում եմ կոդը, արհեստական բանականությունը և դիզայնը՝ ստեղծելու հիանալի պրոդուկտներ և լուծելու բարդ խնդիրներ։",
        hero_btn_projects: "Իմ նախագծերը",
        hero_btn_contact: "Կապվել",
        about_title: '<span class="text-gradient">Իմ</span> մասին',
        about_subtitle: "Բազմակողմանի ՏՏ զինվոր",
        about_p1: "Ես չեմ սահմանափակվում մեկ ոլորտով։ Իմ մոտեցումը խնդիրների լուծումն է ցանկացած արդյունավետ եղանակով։ Ես զբաղվում եմ.",
        about_li1: "Ժամանակակից վեբ և բջջային հավելվածների մշակում",
        about_li2: "Պրոֆեսիոնալ վիդեոմոնտաժ",
        about_li3: "Արհեստական բանականության ներդրում բիզնեսում",
        about_li4: "Նախագծերի ստեղծում No-Code հարթակներում",
        about_li5: "Համակարգիչների վերանորոգում և կարգավորում",
        about_p2: "Հմտությունների այսպիսի լայն շրջանակը թույլ է տալիս ինձ տեսնել նախագծերը ամբողջությամբ՝ գաղափարից մինչև վերջնական ինտերֆեյս։",
        about_cv: "Ներբեռնել CV",
        skills_title: 'Իմ <span class="text-gradient">Հմտությունները</span>',
        skill_video: "Վիդեոմոնտաժ",
        skill_hardware: "Համակարգչի վերանորոգում",
        projects_title: 'Իմ <span class="text-gradient">Նախագծերը</span>',
        project_img: "Նախագծի նկար",
        btn_view_project: "Ավելին",
        proj1_title: "AI Telegram Բոտ",
        proj1_short: "Բազմաֆունկցիոնալ բոտ AI ինտեգրմամբ կոնտենտի գեներացման համար։",
        proj2_title: "No-Code Marketplace",
        proj2_short: "Էլ. առևտրի հարթակ ստեղծված առանց կոդ գրելու 2 շաբաթում։",
        proj3_title: "Կոմերցիոն վիդեոմոնտաժ",
        proj3_short: "Դինամիկ գովազդային հոլովակների շարք մոուշն-դիզայնով։",
        contact_title: 'Մնանք <span class="text-gradient">կապի մեջ</span>',
        form_name: "Անուն",
        form_msg: "Հաղորդագրություն",
        form_btn: "Ուղարկել",
        social_title: "Սոցիալական ցանցեր և հղումներ",
        social_desc: "Պատրաստ եմ նոր նախագծերի և համագործակցության։ Արագ եմ պատասխանում։",
        footer_text: "&copy; 2026. Ստեղծված է սիրով և տեխնոլոգիաներով։",
        btn_github: "Դիտել GitHub-ում",
        btn_live: "Live Demo",
        dynamic_texts: [
            "հավելվածների ծրագրավորող եմ։",
            "ԱԲ մասնագետ եմ։",
            "No-Code ստեղծող եմ։",
            "վիդեոմոնտաժի վարպետ եմ։",
            "սարքավորումների փորձագետ եմ։"
        ],
        proj_updated: "Թարմացվել է",
        proj_private: "Փակ",
        proj_project_word: "նախագիծ",
        proj_no_desc: "Նկարագրությունը դեռ ավելացված չէ։",
        proj_error: "Չհաջողվեց բեռնել նախագծերը։",
        proj_view_profile: "Բացել GitHub պրոֆիլը"
    }
};

let currentLang = 'ru';
window.translations = translations;
window.currentLang = currentLang;

// DOM Elements for i18n
const langSelect = document.getElementById('lang-select');
const elementsToTranslate = document.querySelectorAll('[data-i18n]');
const heroGreeting = document.getElementById('hero-greeting');
const aboutTitle = document.getElementById('about-title');
const skillsTitle = document.getElementById('skills-title');
const projectsTitle = document.getElementById('projects-title');
const contactTitle = document.getElementById('contact-title');

function setLanguage(lang) {
    currentLang = lang;
    window.currentLang = currentLang;
    const dict = translations[lang];

    elementsToTranslate.forEach(el => {
        const key = el.getAttribute('data-i18n');
        if (dict[key]) {
            if (el.tagName === 'INPUT' || el.tagName === 'TEXTAREA') {
                el.placeholder = dict[key];
            } else {
                el.innerHTML = dict[key]; // use innerHTML for cases like &copy;
            }
        }
    });

    // Handle special titles with HTML
    heroGreeting.innerHTML = dict.hero_greeting;
    aboutTitle.innerHTML = dict.about_title;
    skillsTitle.innerHTML = dict.skills_title;
    projectsTitle.innerHTML = dict.projects_title;
    contactTitle.innerHTML = dict.contact_title;
    
    // Update Document Title
    document.title = dict.page_title;

    // Reset Dynamic Typing
    resetTyping();
}

langSelect.addEventListener('change', (e) => {
    setLanguage(e.target.value);
});

// Dynamic Typing Effect
const typedTextSpan = document.getElementById("typed-text");
const typingDelay = 100;
const erasingDelay = 50;
const newTextDelay = 2000;
let textArrayIndex = 0;
let charIndex = 0;
let typeTimeout;

function type() {
    const textArray = translations[currentLang].dynamic_texts;
    if (charIndex < textArray[textArrayIndex].length) {
        typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex);
        charIndex++;
        typeTimeout = setTimeout(type, typingDelay);
    } else {
        typeTimeout = setTimeout(erase, newTextDelay);
    }
}

function erase() {
    const textArray = translations[currentLang].dynamic_texts;
    if (charIndex > 0) {
        typedTextSpan.textContent = textArray[textArrayIndex].substring(0, charIndex - 1);
        charIndex--;
        typeTimeout = setTimeout(erase, erasingDelay);
    } else {
        textArrayIndex++;
        if (textArrayIndex >= textArray.length) textArrayIndex = 0;
        typeTimeout = setTimeout(type, typingDelay + 500);
    }
}

function resetTyping() {
    clearTimeout(typeTimeout);
    typedTextSpan.textContent = '';
    textArrayIndex = 0;
    charIndex = 0;
    typeTimeout = setTimeout(type, 500);
}


// Theme Switcher
const themeToggle = document.getElementById('theme-toggle');
const htmlEl = document.documentElement;

// Load from local storage
const savedTheme = localStorage.getItem('theme') || 'dark';
htmlEl.setAttribute('data-theme', savedTheme);
themeToggle.textContent = savedTheme === 'dark' ? '☀️' : '🌙';

themeToggle.addEventListener('click', () => {
    const currentTheme = htmlEl.getAttribute('data-theme');
    const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
    htmlEl.setAttribute('data-theme', newTheme);
    localStorage.setItem('theme', newTheme);
    themeToggle.textContent = newTheme === 'dark' ? '☀️' : '🌙';
    updateParticlesColor();
});

// Custom Cursor
const cursor = document.querySelector('.custom-cursor');
let mouseX = window.innerWidth / 2;
let mouseY = window.innerHeight / 2;
let cursorX = mouseX;
let cursorY = mouseY;

document.addEventListener('mousemove', e => {
    mouseX = e.clientX;
    mouseY = e.clientY;
});

function renderCursor() {
    cursorX += (mouseX - cursorX) * 0.5; 
    cursorY += (mouseY - cursorY) * 0.5;
    cursor.style.transform = `translate3d(${cursorX}px, ${cursorY}px, 0) translate(-50%, -50%)`;
    requestAnimationFrame(renderCursor);
}
requestAnimationFrame(renderCursor);

const hoverElements = document.querySelectorAll('a, button, .skill-tag, .project-card, select, input, textarea');
hoverElements.forEach(el => {
    el.addEventListener('mouseover', () => cursor.classList.add('hover'));
    el.addEventListener('mouseout', () => cursor.classList.remove('hover'));
});

// Scroll Animations
const observerOptions = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
};

const observer = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.classList.add('visible');
            observer.unobserve(entry.target);
        }
    });
}, observerOptions);

document.querySelectorAll('.fade-in').forEach(element => {
    observer.observe(element);
});

// Apply interactive effects to dynamically-added project cards (github-projects.js).
function enhanceProjectCards(cards) {
    cards.forEach(card => {
        // 3D tilt + glow tracking
        card.addEventListener('mousemove', e => {
            const rect = card.getBoundingClientRect();
            const cx = rect.width / 2, cy = rect.height / 2;
            const rx = ((e.clientY - rect.top - cy) / cy) * -5;
            const ry = ((e.clientX - rect.left - cx) / cx) * 5;
            card.style.transform =
                `perspective(1000px) rotateX(${rx}deg) rotateY(${ry}deg) scale3d(1.02,1.02,1.02)`;
            card.style.setProperty('--mouse-x', `${e.clientX - rect.left}px`);
            card.style.setProperty('--mouse-y', `${e.clientY - rect.top}px`);
        });
        card.addEventListener('mouseleave', () => {
            card.style.transform =
                `perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1,1,1)`;
        });
        // custom cursor hover state
        card.addEventListener('mouseover', () => cursor.classList.add('hover'));
        card.addEventListener('mouseout', () => cursor.classList.remove('hover'));
        // scroll-reveal
        observer.observe(card);
    });
}
window.enhanceProjectCards = enhanceProjectCards;

// Smooth Scrolling
document.querySelectorAll('.nav-links a[href^="#"], .hero-actions a').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        if (targetId === '#') return;
        
        const targetElement = document.querySelector(targetId);
        if (targetElement) {
            const navHeight = document.querySelector('.glass-nav').offsetHeight;
            const targetPosition = targetElement.getBoundingClientRect().top + window.pageYOffset - navHeight;
            
            window.scrollTo({ top: targetPosition, behavior: 'smooth' });
        }
    });
});

// Modal Logic — project cards are injected by github-projects.js, which
// populates and opens this same modal. Here we keep only the close handlers.
const modal = document.getElementById('project-modal');
const modalClose = document.querySelector('.modal-close');
const modalBackdrop = document.querySelector('.modal-backdrop');

function closeModal() {
    modal.classList.remove('active');
    document.body.style.overflow = '';
}

modalClose.addEventListener('click', closeModal);
modalBackdrop.addEventListener('click', closeModal);

// 3D Tilt Effect for Cards
const tiltCards = document.querySelectorAll('.tilt-card');
tiltCards.forEach(card => {
    card.addEventListener('mousemove', e => {
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        const centerX = rect.width / 2;
        const centerY = rect.height / 2;
        
        const rotateX = ((y - centerY) / centerY) * -5;
        const rotateY = ((x - centerX) / centerX) * 5;
        
        card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`;
    });
    
    card.addEventListener('mouseleave', () => {
        card.style.transform = `perspective(1000px) rotateX(0deg) rotateY(0deg) scale3d(1, 1, 1)`;
    });
});


// Particles Background
const canvas = document.getElementById('particles-canvas');
const ctx = canvas.getContext('2d');
let particlesArray = [];

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let particleColor = htmlEl.getAttribute('data-theme') === 'light' ? 'rgba(0, 119, 255, 0.2)' : 'rgba(0, 240, 255, 0.2)';

window.addEventListener('resize', () => {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
    initParticles();
});

function updateParticlesColor() {
    particleColor = htmlEl.getAttribute('data-theme') === 'light' ? 'rgba(0, 119, 255, 0.4)' : 'rgba(0, 240, 255, 0.2)';
    initParticles();
}

class Particle {
    constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * canvas.height;
        this.size = Math.random() * 3 + 1;
        this.speedX = Math.random() * 1 - 0.5;
        this.speedY = Math.random() * 1 - 0.5;
    }
    update() {
        this.x += this.speedX;
        this.y += this.speedY;
        if (this.size > 0.2) this.size -= 0.005;

        // Wrap around
        if (this.x < 0) this.x = canvas.width;
        if (this.x > canvas.width) this.x = 0;
        if (this.y < 0) this.y = canvas.height;
        if (this.y > canvas.height) this.y = 0;
    }
    draw() {
        ctx.fillStyle = particleColor;
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.fill();
    }
}

function initParticles() {
    particlesArray = [];
    const numberOfParticles = (canvas.width * canvas.height) / 15000;
    for (let i = 0; i < numberOfParticles; i++) {
        particlesArray.push(new Particle());
    }
}

function animateParticles() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    for (let i = 0; i < particlesArray.length; i++) {
        particlesArray[i].update();
        particlesArray[i].draw();
    }
    requestAnimationFrame(animateParticles);
}

// Magnetic Elements
const magneticElements = document.querySelectorAll('.btn, .skill-tag, .theme-toggle, .lang-select, .modal-close, .social-link-item');
magneticElements.forEach(el => {
    el.addEventListener('mousemove', (e) => {
        const rect = el.getBoundingClientRect();
        const x = (e.clientX - rect.left) - rect.width / 2;
        const y = (e.clientY - rect.top) - rect.height / 2;
        
        // Combine with existing transform if it's a skill tag, otherwise just translate
        if(el.classList.contains('skill-tag')) {
            el.style.transform = `translate(${x * 0.3}px, ${y * 0.3}px) scale(1.05)`;
        } else {
            el.style.transform = `translate(${x * 0.3}px, ${y * 0.3}px)`;
        }
    });
    
    el.addEventListener('mouseleave', () => {
        el.style.transform = '';
    });
});

// Glow Tracing Cards
const trackingCards = document.querySelectorAll('.project-card, .about-card, .contact-form-card, .info-card');
trackingCards.forEach(card => {
    card.addEventListener('mousemove', e => {
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;
        card.style.setProperty('--mouse-x', `${x}px`);
        card.style.setProperty('--mouse-y', `${y}px`);
    });
});

// Preloader
window.addEventListener('load', () => {
    const preloader = document.getElementById('preloader');
    if(preloader) {
        setTimeout(() => {
            preloader.style.opacity = '0';
            setTimeout(() => {
                preloader.style.visibility = 'hidden';
                preloader.style.display = 'none';
            }, 500);
        }, 800);
    }
});

// Scroll Progress Bar
const scrollProgress = document.getElementById('scroll-progress');
window.addEventListener('scroll', () => {
    if(!scrollProgress) return;
    const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
    const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
    const scrollPercent = (scrollTop / scrollHeight) * 100;
    scrollProgress.style.width = scrollPercent + '%';
});

// Initialization
document.addEventListener("DOMContentLoaded", function() {
    setLanguage(langSelect.value);
    initParticles();
    animateParticles();
});
